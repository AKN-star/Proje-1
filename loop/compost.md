Read this week's exhaust: FAILED in STATE.md, fails in trust.tsv,
FAILs in goal-ledger.tsv, PRs closed unmerged. Extract AT MOST 3 proposals:
a new CLAUDE.md law (quote incidents), a skill fix (same failure repeating),
or a standing goal we lacked. Propose only. Clean week = say so.

Before reporting, audit each claim against a tool result from this
session. Only report what you can point to evidence for; if a data
source is unavailable (e.g. gh/PR data), say so explicitly instead of
guessing.
